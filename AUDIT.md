# 🏗️ GIGA Standard v5 監査：カベカベ合戦！

- 対象コミット（改修前）：`8033408`
- アーキテクチャ：**B型**（Vite + React、GitHub Pages 配信）
- 測った日：2026-08-04
- 測り方：`tools/measure-ui.mjs` / `tools/measure-pwa.mjs` / `tools/measure-a11y.mjs` /
  `tools/measure-icons.mjs`（実ブラウザ = Chromium 1194 / deviceScaleFactor 2 / 5端末幅 × 7画面）
- 再現方法：`npm ci && npm run build && npm run measure`

> **この文書の読み方**
> 「✅」は測って基準を満たしたもの、「❌」は測って基準に届かなかったもの、
> 「未計測」は**測っていない**もの。測っていないものを ✅ とは書かない。

---

## 実測サマリ（前 → 後）

| 項目 | 改修前 | 改修後 |
|---|---:|---:|
| コントラスト基準未満 | **595件**（36種） | **0件** |
| 操作ボタンのタップ領域 44px 未満 | **35件**（1種） | **0件** |
| 320px 幅の横スクロール | **14px**（4画面） | **0px** |
| JS エラー | 0件 | 0件 |
| CSP 違反 | ―（CSP 未導入） | **0件** |
| 更新が押すまで待つか | **❌ 勝手に切り替わる** | **✅ waiting のまま** |
| `offline.html` | **なし** | **あり** |
| maskable セーフゾーン外の中身（192 / 512） | **2.91% / 2.89%** | **0.01% / 0.00%** |
| アイコン6枚 + 未使用の残骸 | **1,241.6 KB** | **30.7 KB**（−97.5%） |
| 初回 JS（gzip 前 / 後） | 214.6 KB / 66.9 KB | 221.6 KB / 68.3 KB |
| 初回に読むもの合計（JS+CSS+HTML+manifest+favicon） | 251.4 KB | **256.5 KB** |
| キーボードだけで指せる手数 | **0手（1手も指せない）** | 全手 |
| 品質ゲート（静的検査） | ―（無し） | **37 / 37 件** |
| ゲートの自己確認（壊したら落ちるか） | ―（無し） | **23 / 23 件** |
| ゲームの決まりのテスト | ―（無し） | **11 件** |

計測画面：せってい／ゲーム 9x9／ゲーム 5x5／あそびかた／カベモード（プレビュー中）／
エラー表示（置けません）／勝利モーダル
計測端末幅：320×568（設計下限）／375×667／810×1080（iPad）／1366×768（Chromebook）／1920×1080

---

## A. 法務・配布

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| A1 | LICENSE 実ファイル | ❌ | ✅ | README に「MIT License 準拠」とあるが実ファイルが無かった。MIT を追加 |
| A2 | .gitignore | ✅ | ✅ | `node_modules/ dist/ *.log .DS_Store` |
| A3 | dependabot.yml | ❌ | ✅ | monthly（npm + github-actions） |
| A4 | README / MANUAL / AUDIT | ⚠️ | ✅ | README のみ在り。MANUAL.md・AUDIT.md を追加 |

---

## B. セキュリティ

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ❌ | ✅ | 未導入だった。導入後、全7画面 × 5端末幅を歩いて **違反 0件・JSエラー 0件** |
| B2 | 秘密情報・IDの直書きなし | ✅ | ✅ | 該当なし（サーバも通信も持たないアプリ） |
| B3 | OAuthスコープ最小 | — | — | 該当なし（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | ✅ | `postMessage` は Service Worker への `{type:'SKIP_WAITING'}` のみ |
| B5 | サーバー側5段ガード | — | — | 該当なし（個人情報を持たない。localStorage も使っていない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | ✅ | `@babel/standalone` / `cdn.tailwindcss.com` / unpkg / jsDelivr いずれも 0件。React も Tailwind もビルド済み |
| B7 | 残る外部資産に SRI と版の固定 | — | — | 該当なし（外部から読むものが1つも無い。Web フォントも使っていない） |

**学校のフィルタリング下での挙動**：外部への通信が1本も無いため、
`cdn.jsdelivr.net` / `fonts.googleapis.com` が塞がれた状態（＝この作業環境そのもの）で
全画面が正常に表示されることを実測した。

---

## C. 堅牢性

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| C1 | LockService + try/finally | — | — | 該当なし（GAS を使っていない） |
| C2 | 自動復旧 | — | — | 該当なし |
| C3 | pagehide で記録確定 | — | — | 該当なし（記録を保存しない。1台を2人で共有して遊びきる設計） |
| C4 | 通信失敗時のリトライと明示 | — | — | 該当なし（通信しない） |
| C5 | localStorage.clear() を使っていない | ✅ | ✅ | `localStorage` の使用箇所 0件 |

---

## D. 表示（Part I §2）

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | ✅ | `index.html:5`。GAS ではないので `code.gs` は該当なし |
| D2 | 100dvh を使用（100vh 単独でない） | ❌ | ✅ | `App.jsx:404` の `min-h-screen`（= `100vh`）が単独だった。`100dvh` + `@supports` フォールバックへ |
| D3 | safe-area-inset を適用 | ⚠️ | ✅ | 8箇所あったが下部固定バーと横向き時のみ。上下左右すべてに適用 |
| D4 | clamp() による fluid type | ❌ | ✅ | `clamp()` 0件だった。主役（見出し・手番表示・のこりカベの数字）に導入 |
| D5 | Canvas に DPR 補正 | — | — | 該当なし（`getContext('2d')` 0件。盤面は DOM と CSS のみ） |
| D6 | 320px 幅で横スクロールが出ない | ❌ | ✅ | ゲーム画面で **14px はみ出し**（`scrollWidth 334 / clientWidth 320`）。原因はプレイヤーパネル2枚の最小幅 |
| D7 | 画像に width/height、150KB以下 | ⚠️ | ✅ | `<img>` は 0個（絵はすべて絵文字）。リポジトリ直下に未使用の 1,102.4 KB の PNG が残っていた → 削除し、元画像は 32.0 KB にして `tools/` へ |
| D8 | **コントラスト 4.5:1 以上** | ❌ | ✅ | **595件 → 0件**（下表） |
| D9 | **タップ領域 44px 以上**（操作ボタン） | ❌ | ✅ | **35件 → 0件**。フッターの「GIGA山」が **45.4 × 15px**。盤面のマスについては下の「直せない制限」を参照 |
| D10 | prefers-reduced-motion（.01ms） | ✅ | ✅ | `styles.css` に `0.01ms`。`0` ではない |
| D11 | forced-colors 対応 | ❌ | ✅ | 0件だった。ハイコントラストモードで盤面のマス目・カベ・コマの区別が付かなくなる |
| D12 | 提示モード | ❌ | ✅ | 2人対戦だが電子黒板に映して全体で1局を追う使い方があるため追加した |
| D13 | 印刷CSS | — | — | 該当なし（印刷する画面が無い）。ただし誤って印刷したとき固定バーが被らないよう最小限の `@media print` を入れた |
| D14 | 拡大を禁止していない | ✅ | ✅ | `user-scalable=no` / `maximum-scale` ともに 0件 |

### D8 の内訳（改修前・実測 36種 595件）

比の低い順。**`rt`（ふりがな）の色決め打ちが最悪で 1.07** だった。

| 比 | 必要 | 件数 | 文字色 | 背景 | 何の文字か |
|---:|---:|---:|---|---|---|
| **1.07** | 4.5 | 5 | `#6b7280` | `#2563eb` | 「ゲーム**かいし**！」ボタンのふりがな |
| **1.07** | 4.5 | 15 | `#6b7280` | `#3b82f6` | 下部の「**ある**く」ボタンのふりがな |
| **1.28** | 4.5 | 60 | `#6b7280` | `#ef4444` | 赤チームパネル「**ひだり**へ**すす**め！」のふりがな |
| **1.31** | 4.5 | 60 | `#6b7280` | `#3b82f6` | 青チームパネル「**みぎ**へ**すす**め！」のふりがな |
| **2.12** | 4.5 | 10 | `#6b7280` | `#22c55e` | 勝利モーダル「もう**いっかい あそ**ぶ」のふりがな |
| **2.28** | 4.5 | 20 | `#ffffff` | `#22c55e` | 勝利モーダルのボタン本文 |
| **2.43** | 4.5 | 45 | `#9ca3af` | `#f9fafb` | **「光るマスをタップ！」**（＝いま何をすればよいかの案内） |
| **3.08** | 4.5 | 30 | `#d97706` | `#fefce8` | 「**お**すとヨコ」（カベの向きの切り替え説明） |
| **3.68** | 4.5 | 120 | `#ffffff` | `#3b82f6` | 青チームパネルの帯「みぎへすすめ！」本文 |
| **3.76** | 4.5 | 120 | `#ffffff` | `#ef4444` | 赤チームパネルの帯「ひだりへすすめ！」本文 |
| **3.90** | 4.5 | 2 | `#6b7280` | `#e5e7eb` | 下部「カベ」ボタン（ホバー時） |
| **4.39** | 4.5 | 43 | `#6b7280` | `#f3f4f6` | 下部の「あるく」「カベ」ボタン、「とじる」ボタン |
| **4.44** | 4.5 | 50 | `#6b7280` | `#eff6ff` | 手番バナー「**あお**チームの**ばん**です」のふりがな |

**この形は Part I §4 が指摘しているとおりだった。**
ふりがなが要るのは低学年の児童である。つまり**いちばん読めなくて困る人が、いちばん読めない**。
しかも1か所ずつ潰す作りではなく、`R` コンポーネントが1つあるだけなので、**まとめて継がせれば全部直る**。

「光るマスをタップ！」も §2-8 の指摘そのままで、**案内文がいちばん読みにくかった**。

### D9 の内訳（改修前・実測）

| 要素 | 実測 | 出現 |
|---|---|---:|
| フッターの「GIGA山」リンク | **45.4 × 15 px** | 全35画面 |

`button { min-width:44px; min-height:44px }` はグローバルに当たっていたため、
ボタン類はすべて 44px を満たしていた。漏れていたのは `<a>` だけである。
（Part II Phase 3 の「このリンクは既定で小さい。`tap-44` を必ず付ける」に一致）

### D6 の内訳（改修前・実測）

| 端末幅 | 画面 | scrollWidth | clientWidth | はみ出し | 原因 |
|---|---|---:|---:|---:|---|
| 320px | ゲーム画面・あそびかた・カベモード・エラー | 334 | 320 | **14px** | `.panel-row` の中の `.player-panel` 2枚（`div.flex-1`）が 320px に収まらない |

#### 縦スクロールは残っている（前より減ってはいる）

**改修前のコミット（`8033408`）を実際にチェックアウトしてビルドし、同じ条件で測った値**である。
測っていない状態について「改修前は壊れていた」とは書かない。

| 端末幅 | | 横スクロール | 縦スクロール | 盤の幅 | パネルの高さ |
|---|---|---:|---:|---:|---:|
| 320×568 | 改修前 | **14px** | 200px | 288px | 188px |
| 320×568 | 改修後 | **0px** | 177px | 292px | 159px |
| 375×667 | 改修前 | 0px | 156px | 343px | 188px |
| 375×667 | 改修後 | 0px | 146px | 359px | 161px |

**縦のスクロールは残る。** 320×568 では、盤を出したうえでプレイヤーパネル2枚を
画面内に収めきれない。盤を 292 → 約 224px まで縮めれば収まるが、
1マスが 25.3 → 19px になり、いまより押しにくくなる。**盤を優先して残した。**

Part I の下限条件は「**横**スクロールが発生せず、全機能に到達できること」で、
縦に少しスクロールすれば のこりカベの枚数まで届くため、条件は満たしている。

---

## E. PWA（Part I §3）

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| E1 | manifest の id/scope/start_url | ⚠️ | ✅ | `id` だけ `/Quoridor/`、`scope`/`start_url` は `"./"` と不揃いだった。3つとも `/Quoridor/` に統一 |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | ✅ | `apple-touch-icon.png` の**透明画素 0.00%**（180×180）。iPad で四隅が黒くなる問題は無い |
| E3 | beforeinstallprompt を head 最上部の外部ファイルで捕捉 | ❌ | ✅ | 捕捉していなかった。`install-hook.js` を `<head>` 最上部で同期読み込み |
| E4 | インストールボタン（案内できるときだけ） | ❌ | ✅ | 無かった。合図を受け取ったときだけナビに出す |
| E5 | 自アプリ接頭辞のキャッシュのみ削除 | ✅ | ✅ | **実測**：`other-app-v1` と `kanji-town-static-v2` を置いてから版を上げ、両方とも残存を確認 |
| E6 | sw.js が localStorage に触れていない | ✅ | ✅ | 使用 0件 |
| E7 | 更新通知（押すまで切り替わらない） | ❌ | ✅ | **`install` の中で `skipWaiting()` していた**。版を上げて `registration.update()` → 3秒放置しても `waiting: false`（＝勝手に入れ替わっていた）。撤去して更新トーストを追加 |
| E8 | 初回訪問で勝手にリロードしない | ✅ | ✅ | **実測**：まっさらな状態で `framenavigated` を数えて **1回** |
| E9 | Service Worker が実際に登録されている | ✅ | ✅ | **実測**：`getRegistration()` が `{active:true}` を返す |
| E10 | offline.html（外部資産・JS に頼らない） | ❌ | ✅ | **存在しなかった**。本体キャッシュを消してから圏外にすると何も出せない状態だった |
| E11 | APP_VERSION を今回のリリース値に更新 | — | ✅ | `kabe-kabe-v3` → `kabe-kabe-v4` |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ❌ | ✅ | **前 2.91%（192）/ 2.89%（512）→ 後 0.01% / 0.00%**。盤面の左右の角と「カベ合戦！」の文字が円の外に出ていた。絵を 0.62 倍にして下地を端まで伸ばした |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ❌ | ✅ | MANUAL.md が無かった |

### E10 で見つかった別の不具合（読んでも分からなかったもの）

`offline.html` を足して測り直したところ、**圏外での起動そのものが壊れていた**ことが分かった。

```
サーバを止めて再読み込み → タイトルは「カベカベ合戦！」と出るが、盤面が出ない
```

`sw.js` の先読みに `index.html` しか入っておらず、**ハッシュ付きの js / css が入っていなかった。**
初回の読み込みでは Service Worker がまだページを管理下に置いていないため、
js / css の取得が Service Worker を通らず、キャッシュに残らない。
つまり **「一度開いてから圏外へ行った児童」に白い画面が出ていた。**
README の「一度読み込めばオフラインでも遊べる」は、実際には成り立っていなかった。

`vite.config.js` のプラグインで、ビルド後に実ファイル名を `sw.js` へ埋め込むようにした。
埋め込み先が見つからなければビルドを落とす。

### ⚠️ 圏外の測り方（`setOffline` では測れない）

`context.setOffline(true)` を使うと **Chromium では Service Worker の中からの `fetch` が
そのまま通ってしまう。** 本体キャッシュを消しても普通にアプリが表示され、
上の不具合が「直っている」ように見えた。

**配っているサーバを実際に止めて測っている。** これでようやく本物の圏外に近くなる。

### E7 の実測ログ（改修前）

```
版を 'kabe-kabe-v3' → 'kabe-kabe-v999-test' に書き換え → registration.update() → 3秒放置
  → { waiting: false, active: ".../sw.js" }
```

`waiting` に留まらず、そのまま新しい版が有効になっていた。
**児童が対戦している最中に画面が入れ替わり、並べたカベと駒の位置が消える。**
「たまに盤面が最初に戻る」という、先生からは再現できない不具合になる。

---

## F. アクセシビリティ・性能

| # | 項目 | 前 | 後 | 実測 |
|---|---|:--:|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ⚠️ | ✅ | `aria-label` は2つのボタンに在り。`<img>` は 0個。**手番の交代・勝敗・エラーを読み上げる仕組みが無かった**ので追加 |
| F2 | モーダルに role="dialog"・Esc で閉じる | ❌ | ✅ | `role` も `aria-modal` も無く、**Esc も効かず、フォーカスも閉じ込めていなかった**。あそびかたを開くとタブ移動が背面へ抜けていた |
| F3 | キーボードのみで全機能に到達 | ❌ | ✅ | **盤面のマスが `<div onClick>` で、`tabindex` も `role` も無かった。キーボードだけでは1手も指せない状態だった。** 矢印キーでの移動 + Enter/Space で確定に対応 |
| F4 | rt の色を決め打ちしていない | ❌ | ✅ | `App.jsx:93` で `text-gray-500` を決め打ち。D8 参照 |
| F5 | 初回JS 300KB以下 | ✅ | ✅ | **221.6 KB**（gzip 68.3 KB）。CDN からの取得 0 バイト。初回に読むもの全部でも 256.5 KB |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | ✅ | 最大 `App.jsx` **811行 / 43.7 KB** |

---

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | — | 該当なし。**このアプリは何も保存しない**（`localStorage` 使用 0件）。得点や記録を残す設計ではないため `study.v1` の対象外 |
| G2 | 中断記録・5分ルール | — | 同上 |

---

## 直せない制限（測ったうえで、直さないと決めたもの）

### 9x9 の盤は、小さい画面で 1マス 44px にならない

| 端末幅 | 盤 | 1マスの実測 |
|---|---|---:|
| 320×568 | 9x9 | **25.3 px** |
| 375×667 | 9x9 | **32.8 px** |

盤の高さは画面の高さから決まるため、320×568 では盤全体が 292px にしかならない。
9マスで 44px ずつ取るには盤だけで 400px 以上要るので、**どう組んでも届かない。**

改修前はこれが**計測に出てこなかった**。マスが `<div onClick>` で `role` も `tabindex` も
無く、支援技術からも計測からも「操作できるもの」に見えていなかったためである。
キーボードで操作できるようにしたことで、初めて数字として見えるようになった。

補っている手段：
- 5x5（かんたん）・7x7（ふつう）を選べる（既定は 9x9 だが、低学年には 5x5 を MANUAL で勧めている）
- キーボード（矢印 + Enter）で指せる
- 押しまちがえても「待った！」で何手でも戻せる

`tools/measure-ui.mjs` では操作ボタンと盤面のマスを分けて数え、
**操作ボタン 0件**を判定に使い、盤面の実寸は参考値として別に出している。

### 品質ゲートの自己確認で、検査そのものの不具合が3件見つかった

「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない。
`npm run gate:self-test` でわざと壊して確かめたところ、次が見つかった。

| 検査 | 何が起きていたか |
|---|---|
| `D2_DVH` | 「100vh の近くに 100dvh があれば通す」作りだった。正しい書き方では 100vh の直後に `@supports` のフォールバックが続くため、**壊れた 100vh のほうまで素通り**していた |
| `E7_NO_SKIP_WAITING_IN_INSTALL` | install ブロックを正規表現で切り出していたが、実際の `sw.js` の形にどこも当たらず常に `null` になっていた。**この検査は何も見ていなかった** |
| `E3b_INSTALL_HOOK_FIRST` | コメントを落とさずに `install-hook.js` を探していたため、説明文に反応して `<script>` を消しても通っていた |

いまは 23 / 23 件が「壊したら落ちる」ことを確認できている。

---

## 測っていないもの（重要）

**測っていないものを ✅ とは書かない。** 次は今回**測れていない**。

| 測れていないこと | 理由 |
|---|---|
| **本番（`gigayama.github.io/Quoridor/`）での動作** | この作業環境から `gigayama.github.io` へ到達できない（プロキシ 403）。すべて `dist/` をローカルに配って測っている |
| **実機の Chromebook / iPad での表示** | 実機が無い。Chromium の deviceScaleFactor 2・指定解像度での代替測定 |
| **iOS Safari の挙動**（ホーム画面追加、ITP、`beforeinstallprompt` の不在） | WebKit を実機で動かせない。Chromium での測定のみ |
| **実際のインストール導線**（`beforeinstallprompt` の発火） | ローカル HTTP では発火条件（HTTPS・エンゲージメント）を満たせない。ボタンは合図を受けたときだけ出す作りで、合図が無ければ出ない側に倒している |
| **電子黒板（4K・65〜75インチ）での見え方** | 実機が無い。1920×1080 と提示モード 150% での代替測定 |
| **校内 Wi-Fi で40人同時に開いたときの初回表示時間（LCP）** | 実測環境が無い。転送量（初回 JS 226.8 KB / 総アセット 297.6 KB）からの見積りに留める |
| **ハイコントラストモード（forced-colors）の実際の見え方** | Chromium の強制カラーをエミュレートしていない。CSS を入れたのみ |
| **prefers-reduced-motion を有効にした状態での全画面走査** | CSS は入っているが、その状態でのコントラスト再走査はしていない |
| **提示モード（`.presentation`）を on にした状態でのコントラストとタップ領域** | 文字が大きくなる方向なので悪化しにくいが、走査はしていない |
| **maskable が実際に丸く切り抜かれた見た目** | 画素で「セーフゾーン外の中身 0.01% / 0.00%」までは測ったが、端末のランチャーに並べた実物は見ていない |
| **提示モードの全画面（`requestFullscreen`）が実機で通るか** | ヘッドレスでは確かめられない。全画面にならなくても文字が大きくなる側は効く作りにしてある |
| **スクリーンリーダー（NVDA / VoiceOver）での実際の読み上げ** | `aria-live` に文字が入ることと、フォーカスの移動までは測った。読み上げ音声そのものは聞いていない |
